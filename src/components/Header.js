function Header() {
    return (
        <header className="page-header">
            <section>
                <h2>React Projects</h2>
            </section>
        </header>
    );
}

export default Header;